import sys; from pathlib import Path; sys.path.insert(0, str(Path.cwd())); from sqlalchemy import create_engine, text; from passlib.context import CryptContext; engine = create_engine("postgresql://postgres:lemon2judy@103.36.221.226:5432/postgres"); conn = engine.connect(); conn.execute(text("COMMIT")); r = conn.execute(text("SELECT 1 FROM pg_database WHERE datname="TrendRadar"")); print("DB exists:\ , r.fetchone() is not None); conn.close()
